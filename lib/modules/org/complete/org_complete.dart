import 'package:flutter/material.dart';

class OrgComplete extends StatelessWidget {
  const OrgComplete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('OrgComplete'),
    );
  }
}

import 'package:flutter/material.dart';

class OrgDone extends StatelessWidget {
  const OrgDone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('OrgDone'));
  }
}
